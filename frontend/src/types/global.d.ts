// Minimal declarations if TS fails to pick up @types/node
// Remove once environment resolves node types automatically.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
declare const process: any;
